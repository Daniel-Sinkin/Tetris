{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "main (Debug)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/main",
      "args": [],
      "cwd": "${workspaceFolder}",
      "stopOnEntry": false,
      "preLaunchTask": "cmake: configure+build (Debug)"
    }
  ]
}